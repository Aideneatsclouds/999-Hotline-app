import 'package:flutter/material.dart';

class SquareTiles extends StatelessWidget {
  const SquareTiles({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
